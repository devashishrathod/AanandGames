const { createGround } = require("./createGround");
const { getAllGrounds } = require("./getAllGrounds");
const { getGround } = require("./getGround");
const { updateGround } = require("./updateGround");
const { deleteGround } = require("./deleteGround");

module.exports = {
  createGround,
  getAllGrounds,
  getGround,
  updateGround,
  deleteGround,
};
